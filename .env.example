# Banco de dados (desenvolvimento: SQLite | produção: PostgreSQL)
DATABASE_URL="file:./dev.db"

# Obrigatório em produção. Gere uma string aleatória longa (ex: openssl rand -base64 32)
JWT_SECRET="gere-uma-string-secreta-e-mude-em-producao"

# URL pública do app (opcional, para links em emails etc.)
# NEXT_PUBLIC_APP_URL=https://relatorio.fun
